name: 2 - Notify about new materials

on:
  workflow_dispatch:
    inputs:
      lesson_id:
        description: 'Lesson ID, for example lesson-1'
        required: true
        type: string

permissions:
  contents: read

jobs:
  notify:
    if: ${{ vars.TELEGRAM_NOTIFICATIONS_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    env:
      SITE_BASE_URL: ${{ vars.SITE_BASE_URL }}
      NOTIFY_WEBHOOK_SECRET: ${{ secrets.NASTYA_ROZA_NOTIFY_WEBHOOK_SECRET }}
      LESSON_ID: ${{ inputs.lesson_id }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Send one notification to the pair Telegram topic
        run: node scripts/prepare-notification.mjs
